@echo off
echo ============================================
echo   This script is no longer needed.
echo   Users provide their own OAuth tokens.
echo ============================================
echo.
echo To restrict which tokens can use the proxy,
echo add them to proxy\allowed_tokens.txt (one per line).
echo.
pause
